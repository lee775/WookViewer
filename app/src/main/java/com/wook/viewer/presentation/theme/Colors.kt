package com.wook.viewer.presentation.theme

import androidx.compose.ui.graphics.Color

/**
 * 욱뷰어 v0.5 — Pencil 디자인 토큰.
 *
 * Brand: 강아지 마스코트 / 부드러운 teal 톤.
 * Light = FileList / Text Viewer
 * Dark  = Bitmap Viewer (PDF/이미지 — 콘텐츠 가독성 위해 어두운 배경)
 */

// Brand
val BrandAccent       = Color(0xFF4FB8A1)
val BrandAccentLight  = Color(0xFFB8E5DA)
val BrandAccentDark   = Color(0xFF2D9982)

// Light (file list / text viewer)
val LightBg           = Color(0xFFFAFAFA)
val LightSurface      = Color(0xFFFFFFFF)
val LightSurfaceAlt   = Color(0xFFF3F4F6)
val LightBorder       = Color(0xFFEEF0F3)
val LightDivider      = Color(0xFFE5E7EB)

// Dark (bitmap viewer)
val DarkBg            = Color(0xFF202225)
val DarkSurface       = Color(0xFF1A1C1F)
val DarkElevated      = Color(0xFF2A2D32)

// Text
val TextPrimary       = Color(0xFF111827)
val TextSecondary     = Color(0xFF6B7280)
val TextMuted         = Color(0xFF9CA3AF)
val TextOnDark        = Color(0xFFFFFFFF)
val TextOnDarkMuted   = Color(0xFF9CA3AF)

// Banner (TEXT_ONLY 안내) — amber 톤
val BannerBg          = Color(0xFFFEF3DC)
val BannerFg          = Color(0xFF92691C)
val BannerAccent      = Color(0xFFB7791F)
